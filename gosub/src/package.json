{
  "name": "gosub-goproxy",
  "version": "0.0.18",
  "description": "GOPROXY implementation that allows Go modules to live in subdirectories",
  "keywords": [
    "golang",
    "goproxy",
    "gosub"
  ],
  "license": "0BSD",
  "author": "Iain Merrick <iain@moreplease.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/more-please/more-stuff.git",
    "directory": "gosub/src"
  },
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "type": "module",
  "files": [
    "README.md",
    "LICENSE",
    "build/index.js",
    "build/index.d.ts",
    "build/goproxy.d.ts",
    "build/gosub.d.ts",
    "build/result.d.ts"
  ],
  "scripts": {
    "format": "biome check --write --unsafe . && markdown-toc -i ../README.md",
    "build": "esbuild index.ts --outdir=build --format=esm --bundle --minify --external:client-zip",
    "lint": "tsc",
    "prepare": "cp -f ../README.md ../LICENSE ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.4",
    "esbuild": "^0.28.1",
    "markdown-toc": "^1.2.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "client-zip": "^2.5.0"
  }
}
