{
  "name": "gosub.test",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "prettier --log-level warn -w .",
    "test": "vitest run && tsc"
  },
  "devDependencies": {
    "prettier": "^3.3.2",
    "typescript": "^5.5.3",
    "unzipit": "^1.4.3",
    "vitest": "^2.0.2"
  },
  "dependencies": {
    "gosub-goproxy": "link:../src"
  }
}
