{
  "name": "gosub.test",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "prettier --log-level warn -w .",
    "test": "vitest run && tsc"
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "^6.0.3",
    "unzipit": "^2.0.1",
    "vitest": "^4.1.4"
  },
  "dependencies": {
    "gosub-goproxy": "link:../src"
  }
}
