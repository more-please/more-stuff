{
  "name": "gosub.test",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "prettier --log-level warn -w .",
    "test": "vitest run && tsc"
  },
  "devDependencies": {
    "prettier": "^3.3.1",
    "typescript": "^5.4.5",
    "unzipit": "^1.4.3",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "gosub-goproxy": "link:../src"
  }
}
