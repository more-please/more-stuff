{
  "private": true,
  "type": "module",
  "scripts": {
    "format": "prettier --log-level warn -w .",
    "test": "vitest run && tsc"
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "typescript": "^5.2.2",
    "unzipit": "^1.4.3",
    "vitest": "^0.34.4"
  },
  "dependencies": {
    "gosub-goproxy": "link:../src"
  }
}
